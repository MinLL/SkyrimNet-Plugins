{
  "name": "skyrimnet-plugins-validator",
  "version": "1.0.0",
  "description": "Scripts for the SkyrimNet-Plugins hub. validate.mjs (structural validation) is run against every dashboard PR by the private skyrimnet-ops repo's hub-review.yml, checked out from main and receiving the PR content as read-only data; build-index.mjs rebuilds index.json. Never installed or run from PR-supplied code.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "js-yaml": "4.1.0"
  }
}
